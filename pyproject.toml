[project]
name = "arene-kuhn"
version = "0.2.0"
description = "Arène expérimentale : agent LLM, jeu obfusqué, 3 conditions mémoire, écart d'exploitation exact"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
